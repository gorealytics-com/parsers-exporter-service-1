[project]
name = "exporter-base"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "redis[hiredis]>=5.0",
    "aiokafka>=0.10",
    "fastapi>=0.110",
    "uvicorn[standard]>=0.27",
    "prometheus-client>=0.20",
    "orjson>=3.9",
    "loguru>=0.7",
]
