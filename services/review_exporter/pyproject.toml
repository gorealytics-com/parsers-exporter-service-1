[project]
name = "review-exporter"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "exporter-base",
    "python-dateutil>=2.8",
    "clickhouse-connect>=0.7",
]

[tool.uv.sources]
exporter-base = { workspace = true }
