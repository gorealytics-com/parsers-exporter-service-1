[pytest]
testpaths = tests
markers =
    integration: Tests requiring real Redis, PostgreSQL, ClickHouse, or Kafka
    slow: Tests that take more than 10 seconds
addopts = --strict-markers
asyncio_mode = auto
